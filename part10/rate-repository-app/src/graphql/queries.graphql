query getRepositories {
  repositories {
    totalCount
    pageInfo {
      hasPreviousPage
      hasNextPage
      startCursor
      endCursor
    }
    edges {
      node {
        ...RepositoryDetails
      }
    }
  }
}

query getRepository($id: ID!) {
  repository(id: $id) {
    ...RepositoryDetails
    url
  }
}

query getAuthorizedUser {
  authorizedUser {
    id
    username
  }
}
